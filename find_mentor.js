$(".category-box").click(function() {
	//TODO: potentially set localData based on category clicked and use that to only display certain rows in the filters page
	window.open("filters.html", target="_self");
});